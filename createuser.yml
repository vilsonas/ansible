- hosts: test
  become: true
  become_method: sudo
  vars:
    users:
      - "testas1"
  tasks:
   - name: sukurti grupe
     group:
      name: semaphore_test
      state: present
   - name: create users
     user:
      name: fawaz 
      comment: admin
      groups: cloudnloud
      state: present
